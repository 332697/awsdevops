pipeline {
    agent any
 stages {
      
  stage('Run Docker Container on Jenkins') {
           steps {
             
                sh 'docker run hello-world'             
            }
        }
  
    }
}
